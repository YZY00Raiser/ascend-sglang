class TestDisaggregationDecodeDp(TestDisaggregationBase):
    """Testcase：<一句话描述用例测试内容>

    [Test Category] Parameter|Model|Interface
    [Test Target] <描述用例关键测试对象，用于后续自动化评估用例覆盖率>
    """

Test Category（测试分类）

Test Target（测试对象），多个对象用英文分号分割。

Parameter

填写被测参数，与对外发布资料中参数名称保持一致。如：--context-length、--kv-cache-dtype
Model

填写被测模型，与对外发布资料中模型名称保持一致。如：vllm-ascend/DeepSeek-V3.2-Exp-W8A8
Interface

填写被测接口，例如：/server_info、/v1/models
